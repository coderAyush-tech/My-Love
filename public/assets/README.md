# Birthday media

- Put the couple video at `public/assets/our-story.mp4`.
- Put timeline photos in `public/assets/memories/` using the filenames in `src/data/memories.js`.
- Update the image paths in `src/data/memories.js` if you prefer different filenames.
